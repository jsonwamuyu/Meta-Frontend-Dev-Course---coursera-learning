import React from "react";

const ThemeContext = () => {
  const ToggleTheme = () => {};

  return (
    <div>
      <button onClick={ToggleTheme}>Toggle Theme</button>
    </div>
  );
};

export default ThemeContext;
